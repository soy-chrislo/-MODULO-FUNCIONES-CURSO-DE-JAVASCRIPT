import barrel from './barrel.js'

const { assignment, propertyAccessor, arrowFunction, comma, grouping } = barrel

assignment()
propertyAccessor()
arrowFunction()
// * Comma operator
console.log('Comma operator')
comma()
// * Grouping operator
console.log('Grouping operator')
grouping()